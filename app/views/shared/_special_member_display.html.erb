<tr>
  <th>タグ</th>
  <td><%= member.tags.map(&:name).join(", ") %></td>
</tr>
<tr>
  <th>対応地域</th>
  <td><%= member.regions.map(&:name).join(", ") %></td>
</tr>
<tr>
  <th>時給</th>
  <td><%= member.price_per_hour %>円</td>
</tr>